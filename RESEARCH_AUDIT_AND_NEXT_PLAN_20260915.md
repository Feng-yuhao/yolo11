# 研究记录纠错与后续执行依据（2026-09-15）

本文件替代此前聊天中互相冲突的训练建议。它是研究计划，不改变任何代码、历史报告或运行中的任务。当前训练身份根据交付包核对；服务器实际启动命令和本轮 args.yaml 尚未重新读取。

**2026-09-15 E10-clean正式结果更新：下文原“阶段B固定空间权重正式对照”和“资源边界”已被文末的更新计划取代。该对照仍可作为最终机制消融，不再是寻找下一种有效方法的前置训练。**

## 研究目标

无人机航拍图像的多尺度目标检测，采用单模型、单次全图前向的网络结构改进。P2是已有高分辨率基底，不宣称原创。考察总体精度、各尺度表现与计算代价。现有任务解耦细节分支是探索候选，不能因为名字或单次最高mAP直接确定论文最终模型。

“多尺度检测”不等于打开 multi_scale 训练选项；不改变当前1024、batch8、200轮、SGD、TAL2协议。全部禁止自动关机。当前主工作区是yolo11_backup；yolo11v1仅作为只读历史证据。

## 已核实的错误及修正

1. 将E10-clean seed1误称为E10a seed2。交付包启动的是yolo11s_p2_hsdg_clean_img1024_seed1.py，导入的E1 runner默认SEED=1。最终以服务器args.yaml为准。结构精简和随机种子复现不能混为一谈。
2. 先建议中止、后建议继续，没有依据剩余训练量和实验用途作一致决策。当前clean已是一个有明确作用的在跑实验，按原配置完成；不因聊天中的新说法切换训练。
3. 把D10推理时关闭分支的结果说成“证明分支训练时无用”。该结果只说明特定检查点对该分支的敏感性，不能替代重新训练消融。
4. 把多个30轮微调失败推断为相应架构从预训练初始化200轮也不可能有效。E11/E12/E13等从E10a best.pt微调，与完整结构训练的证据不同。失败只否定被测试的协议。
5. 先凭最高mAP选E16a，后因AP-small下降完全否定E16a。两种说法都过强。它在旧tiny目标下失败，但可以保留为整体检测候选；需要机制证据和一致评估，不能改名后声称原假设成立。
6. 忽略E16a门控诊断的NaN/null。预测评估有有限结果，不能据此断言模型训练坏了；也不能用缺失的诊断声称门控工作正常。需要在副本上核查诊断路径、精度和输入预处理。
7. 混用Ultralytics总体mAP和独立COCO式面积评估来解释增益来源。AP-large不是总体mAP的可加分项；当前两条评估甚至可能给出不同的候选排序。
8. 建议的“低通下采样+门控细节回流”与历史E7a/BDSI明显重叠。E7a做过P3->P2和P2->P3双路，后者使用固定低通、步长2采样；200轮整体增益接近零。新建议虽有串行/P4扩展差异，但没有足够新证据支持再次投入。
9. 忽略YOLO neck原有P3->P4->P5下行路径。“网络没有细节回流”的说法错误；E1附加P2支路未参与原PAN回流是更具体的事实。
10. 根据看起来顺畅的文字叙事就声称有创新。双向金字塔、门控融合和任务解耦均有大量先例，必须比较明确算子差异和实际贡献。
11. 把0.2/0.4/0.8个百分点当成论文接收门槛。这些最多是事先制定的资源筛选规则，不能代表SCI/EI要求，也不能证明统计显著性。
12. 在E0/E1/最终模型的多seed安排上反复变动。E0与最终模型重复验证总效果；E1与最终模型配对重复验证新增方法超过P2的效果。两者服务不同主张，不能用前者替代后者。
13. 把“换成多尺度表述”说成优化更容易。任务表述变宽不增加模型中的信息，也不保证收益。性能、机制和论文定位应分别核实。
14. 对速度作无依据归因。历史E10a实际14.10小时，E16a17.24小时。删描述/池化分支不等于删高分辨率主计算。当前15小时原因尚未有服务器逐轮计时证据。

## 核实的指标（百分数，同一列内比较）

| 实验 | Ultralytics mAP50-95 | COCO式AP_all | AP-small | AP-large |
|---|---:|---:|---:|---:|
| E0 | 31.975 | 28.983 | 19.644 | 54.858 |
| E1 | 33.822 | 31.129 | 22.634 | 50.698 |
| E5a DySample | 34.054 | 31.443 | 22.807 | 53.746 |
| E7a BDSI | 33.838 | 31.078 | 22.354 | 49.523 |
| E10a DSSG | 34.113 | 31.500 | 22.892 | 56.635 |
| E15a P1SDI | 34.222 | 31.441 | 22.913 | 56.056 |
| E16a TSDR | 34.431 | 31.376 | 22.575 | 57.708 |

全部为seed1；E10-clean尚无本轮最终结果。E16a相对E10a：Ultralytics +0.318 pp，COCO式AP_all -0.124 pp，AP-small -0.317 pp。不能从此直接宣布哪个评估器有bug。记录注明两者使用独立预测过程，padding、匹配与AP实现不同。当前面积评估采用转换YOLO标签，不是带原生ignore处理的官方VisDrone协议。

E10a相对E1增加0.291 pp；相对E5a仅增加0.059 pp。因此“相对E0总共+2.138 pp”不能全部归因于原创DSSG。

## 可执行的后续顺序

### 阶段A：完成正在跑的E10-clean并锁定评估口径

- 原样完成当前200轮，报告真实架构、seed、初始化来源。不中途修改共享模块或训练超参数。
- clean的研究问题是删去弱敏感通道分支后能否保持效果和降低成本；并未预设它一定涨点或一定持平。
- 使用现有E0/E1/E5a/E10a/E16a及新clean权重作一次统一评估整理。统一图像清单、标签、FP32、padding、conf、NMS、max_det；每个模型的各尺度AP来自同一份预测。
- 保留原Ultralytics主指标，不能见到反转后改用更有利的指标。另列统一COCO式AP/面积指标作解释。如与外部官方VisDrone数字比较，必须补原始注释ignore规则对应的评估；旧结果不覆盖。
- 这一步只复用检查点/预测，不是新的30轮或200轮训练；不安排阈值扫描。它解决真实的评估口径分歧。

### 阶段B：只做一个检验核心贡献的正式对照

主结构以E10a为参照。若clean保持主指标（预先接受至多0.1 pp损失）且计算成本下降，则将clean作为精简候选；该容忍度是工程选择，不是统计等价证明。

下一次新增200轮训练是所选语义引导结构的“固定空间权重对照”：

- 保留相同P4->P3和P3->P2投影、卷积细化、残差连接、Detect及初始化规则。
- 将两个学习型空间门控替换为固定0.5，其余可学习残差尺度保留，且初始值相同。
- 若参照是原E10a，其通道路径也保持原样，避免一次更改两个因素。
- 从同一原始yolo11s.pt及相同新增共享层初始化独立训练seed1、200轮。不是加载已训练检查点关闭门控。
- 比较：同样的跨层融合，学习型空间选择是否比固定权重更好；记录门控额外代价。
- 不把该对照包装成性能新模块；它直接决定“自适应选择”能否作为核心贡献。D10冻结关闭不能代替这个对照。

如果学习型门控与固定权重的结果接近，不能声称注意力机制显著有效；优先记录“普通跨层融合可能解释大部分收益”。这时暂停以该机制为核心的投稿承诺，先评估创新性缺口，不自动启动更多叠加模块。

### 阶段C：复现有价值的候选

- 多seed先做E1 seed2与所选候选seed2这一对，用于确认新增结构超出P2。若模型已定，这两次训练同时用于论文复现表，非另开实验路线。
- 候选相对E1两个seed方向不一致时，不选最好seed作结论；边际增益与波动相当时，只补这对seed3来澄清，不扩展新架构。
- 候选有一致收益后，E0与最终模型补齐seed1/2/3，报告所有结果和均值/标准差。E1是否补seed3取决于论文是否主张跨seed稳定的模块增益，以及seed1/2差异是否仍模糊。
- E10a和clean均不是只因命名就被认定最终模型。E16a保留性能候选记录；未解决评估分歧与tiny监督机制矛盾前，不启动其新变体或自动加入最终方法。

### 阶段D：泛化、对比与写作

- 第二数据集至少先做E0与最终模型，相同训练预算；若要证明新模块而非P2在该数据集有效，还需E1。固定数据划分，检查序列/相邻帧泄漏，先测每轮耗时再分配天数。
- 在统一环境评估参数量、GFLOPs、batch1实际延迟与显存；不能把FP32 batch8验证速度写成部署FPS。
- 现有E5a是低成本现成方法对照，应纳入。不能只选明显更弱的历史方法。
- 对外部论文数字，核对模型规格、输入尺寸、数据划分、预训练和评估协议；不直接比较不同口径的涨幅。
- 在已有大量val反馈的情况下，最终泛化结论应尽可能使用未参与设计的测试集；不再按测试结果修改模块或阈值。
- 实验表、方法图、失败案例与写作可以并行准备。投稿级别和毕业要求须与导师/学校认可目录确认；没有固定涨点数能保证录用。

## 资源边界与停止规则

当前clean完成 + 一次固定权重正式对照是最近的新增结构训练预算。其后先做E1/候选配对seed2，再投入最终多seed和第二数据集。没有依据不启动闭环新模块、E16b或新的切片路线。此处限制的是无证据扩展，不把“再跑一次必成功”当作承诺。

当前研究可写的已知事实：P2带来较大总增益，E10a提供幅度较小、值得核实的跨尺度改进。当前尚未证明：门控独立创新强、E16a全面更优、精简必提速、闭环能进一步上涨、现有证据已经达到某个期刊接收标准。

## E10-clean正式结果与更新的下一轮顺序（2026-09-15）

E10-clean训练完整200轮，seed1、原yolo11s.pt、VisDrone数据、1024/batch8、SGD、TAL2 chunk2、vGPU及最终评估参数与原E10a一致；二者均迁移493个原始预训练张量。原E10a与clean的公共head、loss、TAL helper哈希相同，注册文件tasks.py哈希不同，因此不能把每个微小数值差异都严格归因于删分支。clean best epoch=83，原E10a best epoch=78。

| 指标 | E1 | 原E10a | E10-clean |
|---|---:|---:|---:|
| Ultralytics mAP50-95 (%) | 33.822 | 34.113 | 34.098 |
| COCO式AP_all (%) | 31.129 | 31.500 | 31.222 |
| AP_small (%) | 22.634 | 22.892 | 22.757 |
| AP_medium (%) | 41.900 | 42.485 | 42.401 |
| AP_large (%) | 50.698 | 56.635 | 55.765 |
| 推理参数（融合后） | 9,662,984 | 9,779,258 | 9,750,394 |
| GFLOPs，1024 | 86.723 | 90.776 | 90.717 |

clean相对原E10a的Ultralytics mAP仅-0.015个百分点，但AP_all -0.278、AP_small -0.135、AP_large -0.870个百分点；仅减少28,864个参数和0.059 GFLOPs，同vGPU的batch8/FP32验证推理6.625ms对原E10a 6.595ms，没有可确认的速度收益。E1使用旧4090，不能比较其时间和显存。原E10a的D10冻结检查发现P4/P5通道路径被关闭时结果几乎不变；重新训练clean的面积AP下降，说明冻结干预不等于训练消融，但也不能用一次seed1把下降确定为该分支的重要贡献。

E1、原E10a和clean在约第78-83轮达到较高内置验证mAP，此后训练分类loss继续下降、验证分类loss回升，到200轮内置验证mAP都下降。这是共同训练协议下的泛化/优化现象，不是clean特有故障；尚不能确定是数据难度、标签噪声还是优化超参导致。继续投入完整200轮的固定门控消融主要验证E10a机制，不能改善其当前弱增益，故后置。

**已核实的结构位置限制：** 原E10a和clean先在layer15/16生成neck P3，并由原P3 layer16进入layer17的PAN下行及layer23/24/25的P2上采样、Concat和C3k2融合；增强P3只在layer26生成，增强P2在layer27生成，二者到Detect layer28才被使用。因此晚期增强不会重新参与P2初始融合，也不会重新塑造PAN的P4/P5。这能解释为何后置门控可能受限，但不是性能下降的因果证明。E17a要把干预点移到Concat12/15之前，输出沿原neck传播。

**明确的下一次新增训练候选：E17a，E1上的“融合前任务准备”实验。** 新网络只在原head两次top-down Concat之前处理backbone侧特征：P4 lateral=layer6与上采样P5在concat12前，P3 lateral=layer4与上采样neck P4在concat15前。每处用同一种轻量残差adapter把本地lateral与上采样来源投影到可比较通道，计算位置兼容性，只校准lateral输入；保留原Concat、C3k2、PAN、P2支路、四头Detect、nearest和TAL2。adapter须近恒等初始化，不能从已训练E10a best微调。两处校准后的lateral接共享且只在训练期存在的辅助分类/定位监督，目标来自真实标注，并按特征步长屏蔽不可可靠分辨的实例；辅助权重固定且通过单batch主/辅梯度量级检查，不能在验证集上扫描。P2的256×256密集辅助头不作为默认，以控制32GB显存。辅助分支在推理时移除。

这是一项**机制候选而非已证明原创的方法**：AugFPN已有融合前一致性监督，FaPN已有特征对齐/选择；投稿前必须核对E17a adapter、尺度有效监督与相关工作的具体差异。E17a把“输入特征是否适合融合”作为新问题，而不是再把门控放在融合后的Detect前。其效果并没有高概率保证。

执行顺序：用现有checkpoint复核一次同图像、同padding、同conf/NMS的一份预测来计算总体/面积指标，尤其解决E10a与E16a两个评估器排序冲突；这是一次只读评估，可与E17a代码准备并行，不启动新训练，也不为有利数字改主指标。E17a代码完成后依次check-only、2-epoch显存/梯度smoke、一次从相同原始权重启动的正式训练。正式筛选固定最大epochs=200、原200轮cosine学习率日程、patience=50、其余原协议不变；这一预先声明的早停可能丢掉更晚才出现的收益，须报告实际训练轮数，并让最终论文对照采用匹配的patience设置。现有E1/E5a/E10a是独立比较对象，不能只对较弱E1报涨幅；E1/E5a历史seed1来自旧4090，E17a来自vGPU，其seed1性能比较是筛选线索，不是严格同硬件因果证据。

E17a seed1若没有同时呈现清晰的总体与统一AP_all改善，或者小/中/大目标发生明显代价，则止步，不自动组合E10a、E16a或再叠注意力。若超过已有E5a/E10a的性能-成本折衷，先补同vGPU、同早停设置的E1 seed1对照，再做E1/E17a seed2配对和仅一个必要的adapter-only消融，以区别普通适配与辅助监督；之后才谈E10a组合、第二数据集和最终模型多seed。0.1-0.2个百分点的单seed波动不能直接用来承诺论文。固定门控对照只在E10a被选作最终核心方法时补做。

有限服务器时间内，优先完成一个针对新瓶颈的完整正式实验，并在失败时停线；没有实验方案可以保证在半个月内产出SCI/EI可发表增益。独立辅助监督、普通门控或已有P2也都不能单独包装成新创新。

## 主要证据路径

- yolo11_project/comparison_reports/e10a_yolo11s_p2_dssg_img1024_seed1_20260910_222554_227195/{metrics.json,dssg.py}
- yolo11_project/comparison_reports/e16a_yolo11s_p2_tsdr_img1024_seed1_20260914_082236_805369/{metrics.json,tsdr.py,tsdr_loss.py}
- yolo11_project/comparison_reports/DIAG_D10_E10A_DSSG_CAUSAL_20260911_130337_360629/metrics.txt
- E10clean_HSDG_Registered_AutoDL/{README_E10clean_HSDG.md,yolo11_project/yolo11s_p2_hsdg_clean_img1024_seed1.py}
- C:/Users/Fengyuhao/Desktop/yolo11v1/yolo11_project/comparison_reports/e7a_yolo11s_p2_bdsi_img1024_seed1_20260908_153727_685756/metrics.json
- C:/Users/Fengyuhao/Desktop/yolo11v1/yolo11_project/comparison_reports/DIAG_E7A_DIRECTIONS_20260909_090016_544574/diagnosis.txt

## 已核实的相关工作边界

- PANet已有自底向上定位信息传递：https://openaccess.thecvf.com/content_cvpr_2018/html/Liu_Path_Aggregation_Network_CVPR_2018_paper.html
- EfficientDet/BiFPN已有加权双向多尺度融合：https://openaccess.thecvf.com/content_CVPR_2020/html/Tan_EfficientDet_Scalable_and_Efficient_Object_Detection_CVPR_2020_paper.html
- VisDrone原始协议包含ignore/others处理说明：https://github.com/zhaobaiyu/visdrone/blob/master/doc/dataset.md
